#pragma once

// Include standard C header(s).
#include <stdint.h>

// Create explicitly signed fixed-width integer types corresponding to the
// unsigned fixed-width integer types already defined in stdint.h.
typedef int8_t sint8_t;
typedef int16_t sint16_t;
typedef int32_t sint32_t;
typedef int64_t sint64_t;
